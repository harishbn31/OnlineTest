<%= form_with(model: question_bank, local: true) do |form| %>
  <% if question_bank.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(question_bank.errors.count, "error") %> prohibited this question_bank from being saved:</h2>

      <ul>
      <% question_bank.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :question_bank_title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
